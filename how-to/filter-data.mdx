---
title: "Filter data"
description: "필터, 테이블 정렬 등을 이용해 필요한 데이터를 조회할 수 있습니다."
---

### Filter data with input field

- `params`

```yaml
blocks:
  - type: http
    method: GET
    fetchFn: |
      const q = name || 'all'
      return [
        { id: 1, keyword: q, result: 'mock-1' },
        { id: 2, keyword: q, result: 'mock-2' }
      ]
    params:
      - key: name
        placeholder: 검색어 입력
```

### Set input format and component

- `params.format: date`
- `params.dropdown`

```yaml
params:
  - key: date
    format: date
  - key: select
    dropdown:
      - Brut
      - Rosé Brut
```

### Filter multiple blocks

- `pages.params`

```yaml
params:
  - key: name
    placeholder: 검색어
blocks:
  - type: http
    method: GET
    fetchFn: |
      const q = name?.trim() || 'all'
      return [{ id: 1, keyword: q, result: 'srv-1' }]
  - type: http
    method: GET
    fetchFn: |
      // 동일 name 파라미터 재사용
      const q = name?.trim() || 'all'
      return [{ id: 2, keyword: q, result: 'srv-2' }]
```

### Search table in client-level

- `searchOptions`

```yaml
type: http
searchOptions:
  enabled: true
```

### Filter column in client-level

- `columns.[name].filterOptions`

```yaml
type: http
columns:
  name:
    filterOptions:
      enabled: true
```

### Paginate table in client-level

- `paginationOptions`

```yaml
type: http
paginationOptions:
  enabled: true
```

### Sorting table

- `sortOptions`

```yaml
type: http
sortOptions:
  enabled: true
  initialSortBy:
    field: created_at
    type: desc # desc, asc
```

### Group input filter

- `params.group`

```yaml
params:
  - key: name
    group: 1
  - key: email
    group: 2
  - key: phone
    group: 2
```

### Styling filter group

- `formOptions.display`

```yaml
params:
  - key: name
    group: 1
  - key: email
    group: 2
  - key: phone
    group: 2
formOptions:
  display: col # inline, form
  firstLabelWidth: 120px
  labelWidth: 120px
```